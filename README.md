# wnus
Windows Native Unix Shell (wnus prompt) is a powerful Unix-like environment for Windows power users that can work with standard NTFS filesystem using 100% windows native api. It has many *nix commands rewritten and built in.  It requires no external programs for regular use. 140+ commands fully reimplemented with new code.
